services:
  mongodb:
    image: mongo:7
    container_name: taskgrind-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: taskgrind
    restart: unless-stopped

volumes:
  mongo-data:
